pub struct StoreTable {
    path: String,

}

impl StoreTable {
    fn assume_path(path: String) -> StoreTable {
        return StoreTable{
            path,
        };
    }

    fn append(&self) {
        
    }
}
